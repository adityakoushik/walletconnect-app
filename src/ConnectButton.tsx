// src/ConnectButton.tsx

import React from 'react'

// Ensure you have the `w3m-button` component correctly installed and imported.
// If it's a custom element or from a library, you might need to adjust this import.

export default function ConnectButton() {
	return <w3m-button />
}
